general:
  branches:
    ignore:
      - gh-pages

test:
  override:
    - exit 0

deployment:
  staging:
    branch: master
    commands:
      - git config --global user.email "$GH_EMAIL" > /dev/null 2>&1
      - git config --global user.name "$GH_NAME" > /dev/null 2>&1
      - ./scripts/publish
